workflows:
  android-debug:
    name: Android Debug Build

    environment:
      node: 18

    scripts:
      - npm install
      - npm run build
      - npx cap sync android
      - cd android
      - ./gradlew assembleDebug

    artifacts:
      - android/app/build/outputs/**/*.apk
